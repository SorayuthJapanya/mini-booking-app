# Mini Booking App 🎫

ระบบจัดการคิวการจองแบบง่าย พัฒนาด้วย PHP + MySQL + Bootstrap

## คุณสมบัติ ✨

- ✅ หน้าจองคิว (Customer Booking)
- ✅ แดชบอร์ด Admin เพื่อจัดการคิว
- ✅ อัปเดตสถานะการจอง (รอการอนุมัติ, อนุมัติแล้ว, ปฏิเสธ)
- ✅ ฐานข้อมูล MySQL พร้อม PDO

## ความต้องการ 📋

- PHP 7.4 หรือสูงกว่า
- MySQL 5.7 หรือสูงกว่า
- Apache/XAMPP
- PDO Extension

## วิธีติดตั้ง 🚀

### 1. Clone Repository
```bash
git clone <your-repo-url>
cd mini-booking-app
```

### 2. ตั้งค่าฐานข้อมูล
สร้างฐานข้อมูลและตารางด้วยการรัน SQL นี้:

```sql
CREATE DATABASE `mini-booking-app` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE `mini-booking-app`;

CREATE TABLE `bookings` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `customer_name` VARCHAR(255) NOT NULL,
  `booking_date` DATE NOT NULL,
  `booking_time` TIME NOT NULL,
  `status` ENUM('pending', 'approved', 'rejected') DEFAULT 'pending',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3. ตั้งค่า Environment Variables
```bash
# คัดลอกไฟล์ .env.example
cp .env.example .env

# แก้ไข .env ด้วยข้อมูลฐานข้อมูลของคุณ
```

**ตัวอย่าง .env:**
```
DB_HOST=localhost
DB_USERNAME=root
DB_PASSWORD=your_password
DB_NAME=mini-booking-app
DB_CHARSET=utf8mb4
```

### 4. วางไฟล์ใน Web Root

สำหรับ XAMPP:
```
cp -r mini-booking-app/ C:\xampp\htdocs\
```

### 5. เข้าถึงแอปพลิเคชัน

- **Customer**: http://localhost/mini-booking-app/index.php
- **Admin**: http://localhost/mini-booking-app/admin.php

## โครงสร้างไฟล์ 📁

```
mini-booking-app/
├── index.php             # หน้าจองคิวหลัก
├── admin.php             # แดชบอร์ด Admin
├── update_status.php     # API อัปเด้ตสถานะ
├── db.php                # การตั้งค่าฐานข้อมูล
├── navbar.html           # Navbar ร่วม
├── index.css             # Stylesheet
├── .env.example          # ตัวอย่างไฟล์ environment
├── .gitignore            # ไฟล์ที่ข้ามใน Git
└── README.md             # ไฟล์นี้
```

## การใช้งาน 💡

### สำหรับลูกค้า
1. ไปที่หน้า index.php
2. กรอกชื่อ วันที่ และเวลา
3. คลิกจองคิว
4. รอการอนุมัติจาก Admin

### สำหรับ Admin
1. ไปที่หน้า admin.php
2. ดูรายการการจองทั้งหมด
3. อนุมัติหรือปฏิเสธการจอง

## ตัวแปร Environment ⚙️

| ตัวแปร | คำอธิบาย | ค่าเริ่มต้น |
|--------|---------|----------|
| `DB_HOST` | Hostname ของ MySQL | localhost |
| `DB_USERNAME` | Username MySQL | root |
| `DB_PASSWORD` | Password MySQL | (ว่าง) |
| `DB_NAME` | ชื่อฐานข้อมูล | mini-booking-app |
| `DB_CHARSET` | Character set | utf8mb4 |

## ความปลอดภัย 🔒

- ✅ ลบข้อมูล credentials ออกจากโค้ด
- ✅ ใช้ Environment Variables สำหรับการตั้งค่า
- ✅ ไฟล์ `.env` ถูก git ignore
- ✅ ใช้ Prepared Statements ป้องกัน SQL Injection

## การสนับสนุน 💬

หากมีปัญหาหรือคำแนะนำ โปรดสร้าง Issue หรือ Pull Request

## ใบอนุญาต 📄

MIT License

---

📝 **หมายเหตุ**: จำไว้ว่า **ไม่ต้อง** push ไฟล์ `.env` ลงใน GitHub เนื่องจากมีข้อมูลลับ
